name: 'Horusec'
description: 'Run Horusec SAST in your code'
inputs:
  arguments:
    description: Arguments for horusec
    required: false
    default: ""
    
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.ignore_files }}


branding:
  icon: "check"
  color: "orange"